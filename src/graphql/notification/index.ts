import { typeDefs } from "./notification.typedef";
import { queries } from "./notification.queries";
import { mutations } from "./notification.mutations";
import { resolvers } from "./notification.resolvers";

export const Notification = {
  typeDefs,
  queries,
  mutations,
  resolvers,
};
